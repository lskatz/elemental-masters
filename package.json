{
  "name": "elemental-masters",
  "version": "1.1.0",
  "private": true,
  "description": "Browser-based elemental battle game built as a Jekyll site. The game runs as plain ES2020 in the browser; Node is only used for unit tests via the built-in `node --test` runner.",
  "homepage": "https://lskatz.github.io/elemental-masters/",
  "license": "MIT",
  "scripts": {
    "build": "python3 scripts/build.py",
    "test": "node --test tests/js/*.test.js",
    "test:py": "python3 -m pytest tests/",
    "test:all": "npm run test:py && npm test"
  },
  "engines": {
    "node": ">=18"
  }
}
